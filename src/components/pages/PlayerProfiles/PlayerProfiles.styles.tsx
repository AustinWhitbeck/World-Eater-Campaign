import { styled } from '@mui/material';

export const PlayerContentContainer = styled('section')({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  height: 400,
});

export const PlayerSummaryContainer = styled('section')({
  display: 'flex',
  flexDirection: 'column',
  padding: 15,
  marginTop: 20,
});
